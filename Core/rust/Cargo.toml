[package]
name = "stoff_rust"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
geo = "0.32"
getrandom = { version = "0.2", features = ["js"] }
rstar = "0.12.2"

import { Sketch, SVG_Builder } from "../../../Core/index";
import { Recording } from "../index";
import { LiveRecording } from "../recording/recording";

export function put(
    what: Sketch | JSON | Recording.Recording | string | SVG_Builder,
    title: string | null = null,
) {
    if (typeof what == )
}

export function put_async(
    what: Sketch | JSON | Recording.Recording | string | SVG_Builder,
    title: string | null = null,
) {}


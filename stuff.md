Cut => If we cut in, sometimes we have a point becoming two points and sometimes being one point

make ".get_lines" the same as get_lines form teh input things



Goal:

2. Unify rendering
3. Render Data

Editing the design config feels realy bad, bcs you dont get told what is wrong

Eventually we will look at sewing to see how to improve things there for actually making it useable
Eventually observability, speed improvements
- Making them
- Building/Refactoring infra

Figure out build system for Leonie



Next: Unifiy the two differnt parts parts

=> The graphics lib is WLOG about making sewing patterns
=> Alternative maybe construction or so, but that would also be fine

Deal with color gradients (and interpolation) (?)
Get all examples to run


Copy Sketch Data Callback
Remove dublicate methods here/there
Better typing often
Test it still works
redundant methods (like add_point)
Get rid of multi possible initialization for stuff
Get rid of SEC
Allow for lines and points to be treated at thing with out sketch potentially

Sketch overarching functionality

deal with sketch data
inconsistent export (default)
remove anys
make some boolean args string
better color utilities (better gradients)
the drawing section has a lot of redundancy
Sync?

Possible Future types

(especially when going public)
- length

- ToString as const
- Add declarations everywhere to what the things can do.

- Todo:
Move utility methods off thing and let these things be interfaces
Line/Point sketch element array collection


Change around stuff
Remove console logs


Unify PatternLib, StoffLib (?)
Renderer (?)



For dst: Shader!!! i.e. squiggle, make underpath thing/...
